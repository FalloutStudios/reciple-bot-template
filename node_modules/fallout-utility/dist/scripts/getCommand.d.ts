export interface Command {
    command?: string;
    args: string[];
    raw: string;
    prefix: string;
    separator: string;
}
/**
 *
 * Returns the command and arguments from a string
 */
export declare function getCommand(string: string, prefix: string, separator?: string): Command;
