"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.detectCommand = void 0;
/**
 *
 * Detects the string if has the given prefixes
 */
function detectCommand(string, commandPrefix) {
    if (!string || !commandPrefix)
        return false;
    commandPrefix = typeof commandPrefix === 'string' ? [commandPrefix] : commandPrefix;
    for (let prefix of commandPrefix) {
        if (string.startsWith(prefix))
            return true;
    }
    return false;
}
exports.detectCommand = detectCommand;
