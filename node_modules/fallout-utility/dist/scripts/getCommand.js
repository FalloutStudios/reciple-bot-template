"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCommand = void 0;
const detectCommand_1 = require("./detectCommand");
const splitString_1 = require("./splitString");
/**
 *
 * Returns the command and arguments from a string
 */
function getCommand(string, prefix, separator = ' ') {
    const command = {
        command: undefined,
        args: [],
        raw: string,
        prefix: prefix,
        separator: separator,
    };
    if (!(0, detectCommand_1.detectCommand)(string, prefix))
        return command;
    command.command = string.slice(prefix.length).trim().split(' ')[0];
    const args = string.slice(prefix.length).trim().split(' ').splice(1).join(' ');
    command.args = args ? (0, splitString_1.splitString)(args, true, separator) : [];
    return command;
}
exports.getCommand = getCommand;
